# IHaveTheVision
detect text for testing purposes

Install the following packages for the program to function.

pip install opencv-python 

pip install easyocr

pip install torch

paddle install removed due to it not being used in this branch

simply run IHaveTheVision.py and when prompted type the full name of the image whos text you wish to detect and hit enter. the program will print the resulting text to the terminal

There is a branch 

to take a picture from the webcam, you must first have the window with your live webcam feed be focused(click inside the window to focus)

